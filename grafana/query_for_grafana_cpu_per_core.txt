from(bucket: "<INFLUX_BUCKET>")
  |> range(start: -30m)
  |> filter(fn: (r) => r._measurement == "CPU LA (per core)")
  |> filter(fn: (r) => r.source == "${source}")
  |> filter(fn: (r) => r._field == "hrProcessorLoad")

// Dispaly Nameを ${__field.labels.hrDeviceDescr} とすることでCPU名を表示可能
